.row
  .col-lg-12
    .btn-group
      = link_to inventory_index_path, class: 'btn btn-sm btn-default' do
        %i.glyphicon.glyphicon-arrow-left
      = link_to edit_inventory_path(@inventory), class: 'btn btn-sm btn-default' do
        %i.glyphicon.glyphicon-pencil
%hr
  .col-lg-3
    =render 'inventory_info'